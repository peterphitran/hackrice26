# Fixtures

Small sample repositories, webhook payloads, analyzer output, and verification evidence belong here.

`broken-store/` is the deterministic benchmark used by the hackathon demo. Its seed script
creates a temporary Git repository with `good` and `n-plus-one` refs; generated repositories
must not be committed here.

Do not store secrets or large generated artifacts in this directory.
